# Shaver 装配思路

## LoopShaverBody

机身被视为一个带开口的矩形环形手柄。两个实心端段决定开口沿长度方向的位置，中间开口长度决定握持空间，外部深度和侧壁厚度决定环的内外轮廓。整体环再旋转到剃须刀机身的工作方向，作为一个完整的手柄壳体参与最终姿态变换。

## TwoSegmentShaverBlade

刀片由根部连接段和前端刀片段组成。两个段先在同一个 root-local frame 中排布：根部段从旋转根点向外延伸，前端段接在根部段之后并允许有少量竖向错位。随后两段一起绕根点旋转，使刀片整体保持统一的安装角度，而不是分别手算旋转后的中心坐标。
